services:
  mongodb:
    image: mongo:latest
    volumes:
      - mongodb-data:/data
    command: mongod --bind_ip=0.0.0.0
    expose:
      - 27017
    networks:
      - mongodb-network

volumes:
  mongodb-data:

networks:
  mongodb-network: