# Example .env file for local testing with act
# Copy this file to .env and fill in your values
# Usage: act -j build --env-file .env

# Required for SonarCloud scanning
SONAR_TOKEN=your-sonar-token-here

# Required for NuGet package publishing
NUGET_API_KEY=your-nuget-api-key-here

# GitHub token for API access (can be a PAT token)
GITHUB_TOKEN=your-github-token-here

# For simulating branches/PRs with act (optional)
# ACT_BRANCH=main
# ACT_EVENT=push
